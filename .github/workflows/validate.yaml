name: Validación CI - Tests y build sin publicar

on:
  push:
    branches:
      - 'feature/**'
      - 'fix/**'

jobs:
  test-and-build:
    name: Pruebas y build sin publicar
    runs-on: ubuntu-latest
    steps:
      - name: 1 · Descargar el código
        uses: actions/checkout@v4

      - name: 2 · Configurar Node.js v20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 3 · Instalar dependencias
        run: npm ci

      - name: 4 · Ejecutar pruebas
        run: npm test

      - name: 5 · Construir imagen sin publicar
        run: |
          docker build -t demo-api:ci-${{ github.sha }} .
          echo "Imagen construida correctamente - no se publica"
